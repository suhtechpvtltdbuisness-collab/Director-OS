# Director-OS

SUH Director OS frontend — Vite + React.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

Requires the API at `VITE_API_URL` (default `http://localhost:5010`).

## Demo login

- Director: `director@suhtech.top` / `director123` · OTP `123456`
- Manager: `manager@suhtech.top` / `manager123` · OTP `123456`
